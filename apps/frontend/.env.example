# QuickCart frontend environment variables (baked in at build time by Vite).
# The app falls back to http://localhost:4000 when unset, so it runs zero-config locally.

# Base URL of the QuickCart backend API.
VITE_BACKEND_URL=http://localhost:4000
